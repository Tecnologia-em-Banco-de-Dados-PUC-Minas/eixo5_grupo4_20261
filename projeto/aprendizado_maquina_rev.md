# Aprendizagem de Máquina

No primeiro projeto o target foi a quantidade vendida (quantity_sold). Para isso executamos os seguintes algoritmos: Árvore de Decisão, Random Forest Regressor (1.1147678870543194) e XGBoost (1.128473162651062). Por fim, plotamos um gráfico de Comparação Visual das Previsões: Random Forest vs XGBoost. Na próxima etapa faremos as análises dos dados encontrados. Abaixo o nome deste arquivo:

eixo5_grupoa4 (1).py

No segundo projeto para análise inicial da base de dados, utilizamos os modelos de predição random forest, arvore de Decisão e Regressão linear, afim de entender o comportamento dos dados da coluna 'total_revenue', com base nas colunas 'price' e 'product_category' determinando uma taxa de treinamento de 20% da base de dados total, e 80% para o teste dos modelos. Abaixo o nome deste arquivo:

[Análise de Preço](Price_Product_Category.ipynb)

OBS: Os arquivos estão no commit anterior a este.
